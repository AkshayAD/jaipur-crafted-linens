document.addEventListener('DOMContentLoaded', function () {
  const viewer = document.getElementById('viewer');
  if (viewer) {
    viewer.innerHTML = '<p>360 View functionality coming soon.</p>';
  }
});
